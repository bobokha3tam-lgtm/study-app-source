rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // System store is accessed by server.ts (Admin SDK or Client SDK fallback)
    match /system/store {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
